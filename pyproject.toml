[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "systematic_research"
version = "0.1.0"
dependencies = ["pandas", "numpy", "scipy", "scikit-learn", "pyyaml", "pydantic"]